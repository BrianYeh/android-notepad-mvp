# Just Notes v1.0.6-C Cloud / OAuth / Play API Setup Guide

Status: draft for Agent F review
Scope: Console 操作 guide only；不改 implementation、不部署、不由 Agent 建立 Cloud resources
Last updated: 2026-07-06

這份文件是給 Brian 照著做的 Console setup guide。它的目的，是在不開購買流程、
不部署 backend、不中斷 v1.0.6-B non-granting contract 的前提下，先把 dev
backend 之後會需要的 Google Cloud、OAuth、Play API、Secret Manager、KMS、
Firestore、Cloud Run service identity 設定項目整理清楚。

目前 repo baseline：

- Latest commit: `a6c5901 Add backend non-granting verification foundation`
- v1.0.6-B backend-only non-granting foundation 已完成。
- 既有 untracked `Doc/billing/` 不屬於本文件範圍，不要碰。

## 1. Executive Summary

v1.0.6-C 是 Console setup guide，不是 deploy guide，也不是 implementation
change。

本階段仍維持：

- 不開 purchase button。
- 不讓 `launchBillingFlow` reachable。
- Android runtime 不呼叫 `POST /v1/billing/verify`。
- `POST /v1/billing/verify` 仍是 non-granting。
- `POST /v1/billing/verify` 不得讓 `hasPremium=true`。
- 不建立 service account key file。
- 不下載 service account JSON key。
- 不把 secret、credential、raw token 放進 repo。

本階段目標是準備 dev backend 需要的設定：

- GCP project / dev environment 決策。
- OAuth Web client ID。
- Android OAuth client fingerprint 檢查。
- Android Publisher API / Play Console API access。
- Cloud Run runtime service account。
- Firestore Native mode database。
- Secret Manager pepper secrets。
- Cloud KMS key for token encryption。

正式 Cloud Run backend 應使用 runtime service account / service identity，也就是
Application Default Credentials (ADC) 路徑。不要用 repo 裡的 key file，也不要把
service account JSON key 放進 Android app。

## 2. Known Existing Setup

已知 Play subscription 設定：

- packageName: `com.brianyeh.justnotes`
- productId: `just_notes_premium`
- base plans: `monthly`, `annual`
- offer: `monthly` / `trial10d`
- annual 沒有 trial offer。
- Taiwan price / availability 已啟用。

已知 backend 狀態：

- `backend/` 已有 Ktor skeleton。
- 已有 official/fail-closed Google ID token verifier wrapper。
- 已有 Play SubscriptionsV2 lifecycle mapper。
- 已有 package/product/basePlan/offer allowlist。
- 已有 token HMAC / encryption interfaces。
- 已有 ownership validator。
- 已有 acknowledgement / `VerificationPending` no-grant contract。
- `POST /v1/billing/verify` 仍 non-granting。
- `GET /v1/entitlement` 有 TTL / maxStale behavior。
- 沒有 Android runtime POST verify。
- 沒有 purchase flow。
- 尚未建立 Cloud resources。

請明確區分 Drive sync 和 billing backend：

- Drive API / Drive `appData` scope 是 Just Notes Drive sync 用。
- Backend billing verify 需要 OAuth Web client ID、Android Publisher API、
  Cloud Run runtime service account。
- Android `requestIdToken(webClientId)` 必須傳 Web client ID，不是 Android
  OAuth client ID。

## 3. Recommended Dev Setup Values

除非 Brian 決定沿用現有 Just Notes Drive sync project，否則 dev setup 建議如下：

| Item | Recommended dev value |
| --- | --- |
| GCP dev project | `just-notes-dev` |
| Existing project option | 如果現有 project 已用於 Just Notes Drive sync，可先 reuse existing project for dev |
| Cloud Run service | `just-notes-entitlement-api-dev` |
| Cloud Run region | `asia-east1` |
| Firestore | Native mode, region `asia-east1` |
| Cloud Run runtime service account | `just-notes-entitlement-api-dev` |
| Token hash pepper secret | `just-notes-token-hash-pepper-dev` |
| Obfuscated account pepper secret | `just-notes-obfuscated-account-pepper-dev` |
| Email hash pepper secret | `just-notes-email-hash-pepper-dev` |
| KMS key ring | `just-notes-dev` |
| KMS key | `just-notes-token-encryption-dev` |
| KMS region | `asia-east1` |
| GET reverify TTL | 6 hours |
| GET maxStale | 24 hours |

這些值可以先記錄在 Brian 的 private setup notes，之後再由 Agent E 做
deploy-config / env wiring plan。不要 hard-code 到 Android source。

## 4. Console Step-by-Step: Google Cloud Project

目的：確認 dev backend 要使用哪個 Google Cloud project。

操作步驟：

1. 打開 Google Cloud Console。
2. 點上方 project selector。
3. 檢查是否已有 Just Notes 用於 Drive sync 的 project。
4. 如果有現有 Just Notes project，確認：
   - project 屬於 Brian。
   - OAuth consent screen 顯示的是 Just Notes。
   - OAuth clients 對應 `com.brianyeh.justnotes`。
   - Brian 同意 dev backend 先 reuse 這個 project。
5. 如果沒有合適 project，建議 dev project 名稱使用 `just-notes-dev`。
6. 本 guide 階段不要由 Agent 建立 project。
7. 不要先啟用 billing、建立資源或部署服務，除非 Brian 明確同意下一步 setup。

請記錄：

- GCP project display name。
- GCP project ID。
- GCP project number。
- 這是 reused existing project 還是 planned new dev project。
- Project selector / project dashboard 截圖。

判斷原則：

- 如果現有 project 本來就是 Just Notes project，且 OAuth / Play API 會比較單純，
  dev backend 可以先 reuse。
- 如果現有 project 混了其他 app、權限太亂，或不是 Brian 要用的 Just Notes
  backend project，就另外規劃 `just-notes-dev`。

### 4.1 Project Decision Record - 2026-07-08

Project selector / dashboard screenshot:

- Brian confirmed the Google Cloud Console project selector and project dashboard show:
  - Project display name: `Gemini API`
  - Project ID: `gen-lang-client-0599059254`
  - Project number: `1025325431669`

Decision:

- Reuse existing project for Just Notes dev backend planning.
- Do not create `just-notes-dev` for the current planning phase.

Reason:

- This project already has Just Notes OAuth clients:
  - `Just Notes Android Play`
  - `Just Notes Android Debug`
  - Package name: `com.brianyeh.justnotes`
- OAuth consent / data access scope is currently clean:
  - `https://www.googleapis.com/auth/drive.appdata`
- No sensitive scopes or restricted scopes were observed in this check.

Current phase restrictions:

- Do not create a new GCP project.
- Do not enable Cloud Billing.
- Do not enable Cloud Run, Firestore, Pub/Sub, Secret Manager, or KMS.
- Do not deploy a backend.
- Record only the project decision and OAuth / scope verification result.

Revisit this decision if the existing project later proves to mix unrelated apps,
has confusing permissions/resources, or Brian decides this is not the backend
project he wants for Just Notes.

## 5. Console Step-by-Step: Enable APIs

目的：確認 future backend 需要哪些 API。啟用 API 是 Brian 的 Console 操作，不是
Agent 自動建立資源。

需要確認 / 未來需要啟用的 API：

| API | 目的 |
| --- | --- |
| Cloud Run API | 未來部署 backend entitlement API。 |
| Firestore API | 未來儲存 entitlement read model / subscription records。 |
| Secret Manager API | 儲存 token hash、obfuscated account、email hash peppers。 |
| Cloud KMS API | 加密 `tokenCiphertext`，支援 key versioning。 |
| Android Publisher API | 呼叫 `purchases.subscriptionsv2.get` 與未來 backend acknowledge。 |

操作步驟：

1. 到 Google Cloud Console -> APIs & Services -> Library。
2. 逐一搜尋上表 API。
3. 先確認每個 API 是否已 enabled。
4. 若 Brian 已決定要進行 setup，才 enable 上表 API。
5. 這一步只確認 / enable API，不建立 Cloud Run service、Firestore database、
   Secret、KMS key。
6. 不建立 service account key。
7. 不下載 JSON key。

請記錄：

- 每個 API 是 enabled / not enabled。
- API 所在 project ID。
- 若有 enable，記錄日期。
- Console 截圖。

### 5.1 API Enablement Inventory - 2026-07-08

Brian checked the future backend APIs in Google Cloud Console for the reused
project:

- Project display name: `Gemini API`
- Project ID: `gen-lang-client-0599059254`
- Project number: `1025325431669`
- Project decision: reuse existing project for dev backend planning

At this `2026-07-08` API inventory snapshot, each API page showed the blue
`啟用` button, so these APIs were not yet enabled. Brian did not click enable
in that check, so there was no enable date at that time.

| API | Service name | Status at 2026-07-08 | Enabled date at 2026-07-08 | Screenshot |
| --- | --- | --- | --- | --- |
| Cloud Run Admin API | `run.googleapis.com` | not enabled | N/A | provided |
| Google Cloud Firestore API | `firestore.googleapis.com` | not enabled | N/A | provided |
| Secret Manager API | `secretmanager.googleapis.com` | not enabled | N/A | provided |
| Cloud Key Management Service (KMS) API | `cloudkms.googleapis.com` | not enabled | N/A | provided |
| Google Play Android Developer API / Android Publisher API | `androidpublisher.googleapis.com` | not enabled | N/A | provided |

Subsequent approved setup enabled Firestore API on `2026-07-09` and Secret
Manager API on `2026-07-10`; their completion records appear below. Remaining
current restrictions are:

- Do not enable additional APIs without explicit approval.
- Do not create a Cloud Run service.
- Do not create a KMS key ring or key.
- Do not create or download a service account JSON key.
- Do not enable Cloud Billing.
- Do not deploy a backend.

## 6. Console Step-by-Step: OAuth Web Client ID

目的：建立或確認 backend Google ID token verifier 的 audience，也就是 Android
`requestIdToken(webClientId)` 要使用的 Web client ID。

操作步驟：

1. 到 Google Cloud Console -> APIs & Services -> Credentials。
2. 檢查是否已有 OAuth 2.0 Client ID，Application type 是 Web application。
3. 如果沒有，建立新的 Web application OAuth client。
4. 名稱建議：`Just Notes Backend Dev Web Client`。
5. 建立或確認後，記錄 Web client ID。
6. 這個 Web client ID 要給 Android `requestIdToken(webClientId)`。
7. Backend verifier 的 `aud` 也要驗這個 Web client ID。
8. 不要把 Web client secret 放進 Android app。
9. 不要把 Web client secret 放進 repo。
10. 不要把 Web client secret 貼到 chat、docs、logs。

請記錄：

- OAuth client 名稱。
- Web client ID，格式通常是 `xxx.apps.googleusercontent.com`。
- 所在 GCP project ID。
- Console 截圖，secret 欄位不要露出。

Android OAuth client 仍需要確認：

- Debug SHA-1 / SHA-256。
- Release upload key SHA-1 / SHA-256。
- Play App Signing SHA-1 / SHA-256。
- Package name 必須是 `com.brianyeh.justnotes`。

重要區分：

- Android OAuth client 用來識別 Android app 和 signing certificate。
- Android `requestIdToken(webClientId)` 使用的是 Web client ID。
- Backend `GOOGLE_WEB_CLIENT_ID` / verifier `aud` 也使用同一個 Web client ID。
- Android OAuth client ID 不應拿來當 backend verifier 的 audience。

### 6.1 OAuth Web Client Record - 2026-07-08

Brian confirmed this OAuth Web client in Google Cloud Console:

- Project: `Gemini API`
- Project ID: `gen-lang-client-0599059254`
- OAuth client name: `Just Notes Backend Dev Web Client`
- Application type: `Web application`
- Web client ID: `1025325431669-l4rkq07l3f1rq3r7u0hbjd238a0te79p.apps.googleusercontent.com`
- Status: `Enabled`
- Created: `2026-07-08`

Purpose:

- Android `requestIdToken(webClientId)`
- Backend `GOOGLE_WEB_CLIENT_ID`
- Backend verifier `aud` check

Notes:

- This record confirms the Web application OAuth client exists and is enabled.
- The concrete Web client ID was captured on `2026-07-10` for future
  app/backend config wiring.
- Do not store the OAuth client secret in Android, the repo, chat, docs, logs,
  or screenshots.

## 7. Console Step-by-Step: Firestore

目的：建立或確認 future entitlement backend 的 database。這一步只處理 database
setup，不寫入資料。

建議設定：

- Firestore Native mode。
- Region: `asia-east1`。

操作步驟：

1. 到 Google Cloud Console -> Firestore。
2. 確認目前 project 是否已經有 Firestore database。
3. 如果已存在，記錄 mode 和 region。
4. 如果尚未建立，先確認 location。Firestore location 建立後不易搬移。
5. Brian 確認後，選 Firestore Native mode。
6. Region 建議選 `asia-east1`。
7. 本 guide 只建立 / 確認 database，不建立 collections，不寫資料。

Future collections plan：

- `users`
- `subscriptions`
- `entitlements`
- `verificationEvents`
- `auditLogs`

請記錄：

- Firestore mode。
- Firestore region。
- Database 是否已存在。
- 是否新建 database。
- Console 截圖。

Fail-closed 要求：

- Unknown 不 grant。
- `VerificationPending` 不 grant。
- `expiryTime <= now` 不 grant。
- 未 ownership verified 不 grant。
- 未 acknowledged 或 backend acknowledge 未成功不 grant。

### 7.1 Firestore Setup Completion Record - 2026-07-09

Firestore setup is completed in the existing Google Cloud project:

| Setting | Confirmed value |
| --- | --- |
| Project | `Gemini API` / `gen-lang-client-0599059254` / `1025325431669` |
| Project decision | Reuse existing project |
| Setup date | `2026-07-09` |
| Database existed before setup | No |
| Database newly created | Yes |
| Database ID | `(default)` |
| Edition | Standard |
| Mode | Firestore Native mode (原生模式的 Firestore) |
| Region | `asia-east1` (Taiwan) |
| Security rules | Restrictive; mobile and web SDK reads/writes are denied by default |
| Point-in-time recovery | Disabled |
| Scheduled daily backup | Disabled |
| Scheduled weekly backup | Disabled |
| Encryption | Google-managed encryption key |
| Firestore API | Enabled |
| Datastore API | Enabled |
| Collections created | No |
| Documents written | No |

Console evidence provided by Brian:

- Firestore Studio shows database `(default)` and that the database is ready.
- The left navigation offers only `建立集合`; there are currently no collections.
- Brian's Console screenshots are evidence for this record. Do not store any
  secret, API key, OAuth client secret, or token in this document or elsewhere.

Current restrictions after database creation:

- Do not create the future-planned collections or write any documents yet.
- Do not create indexes, configure TTL, enable PITR, or schedule backups.
- Do not create a KMS key, Secret Manager secret, or service account key; do
  not download a JSON key.
- Do not deploy the Cloud Run backend or change Android app code/versioning.

The future collections plan above remains a plan only. Formal entitlement
decisions remain backend-verified and fail closed: unknown state,
`VerificationPending`, `expiryTime <= now`, unverified ownership, or an
unacknowledged purchase / failed backend acknowledgement must not grant
Premium. The Android app must not access Firestore directly.

## 8. Console Step-by-Step: Cloud Run Service Account (Completed 2026-07-09)

目的：建立 future Cloud Run backend 使用的 runtime service account。這個 service
account 是 Cloud Run 的 service identity，不是下載 key 給 app 或 repo 用。

Actual completed service account：

- Display name: `Just Notes Entitlement API Dev`
- ID: `just-notes-entitlement-api-dev`
- Email: `just-notes-entitlement-api-dev@gen-lang-client-0599059254.iam.gserviceaccount.com`
- 用途：Cloud Run runtime service identity。

操作步驟（已完成的 Console record）：

1. 到 Google Cloud Console -> IAM & Admin -> Service Accounts。
2. 選定 dev backend project。
3. 建立 service account：`just-notes-entitlement-api-dev`。
4. Display name：`Just Notes Entitlement API Dev`。
5. 不建立 key。
6. 不下載 JSON key。
7. 不把 key 放 repo。
8. 不把 key 放 Android app。
9. 未來 Cloud Run service 要以這個 service account 作為 service identity。

IAM 最小權限草案：

- Firestore / Datastore document read/write role：只給 backend 需要讀寫文件的權限。
- Secret Manager Secret Accessor：只針對 needed secrets，不要 project-wide 任意擴張。
- Cloud KMS CryptoKey Encrypter/Decrypter：只針對 needed key。
- Android Publisher API 權限由 Play Console API access 管理，不靠 GCP IAM
  owner/editor。

請記錄：

- Service account email。
- Service account unique ID。
- 授權了哪些 IAM roles。
- 每個 role 是 project-level 還是 resource-level。
- Console 截圖。

安全要求：

- 不使用 default compute service account。
- 不使用 owner/editor 當捷徑。
- 不建立 service account key file。
- 不下載 JSON key。

### 8.1 Cloud Run Runtime Service Account Completion Record - 2026-07-09

The runtime service account is completed in the existing GCP project. Cloud Run
itself has not been deployed.

| Setting | Confirmed value |
| --- | --- |
| Project | `Gemini API` / `gen-lang-client-0599059254` / `1025325431669` |
| Project decision | Reuse existing project |
| Setup date | `2026-07-09` |
| Display name | `Just Notes Entitlement API Dev` |
| Service account ID | `just-notes-entitlement-api-dev` |
| Service account email | `just-notes-entitlement-api-dev@gen-lang-client-0599059254.iam.gserviceaccount.com` |
| Status | Enabled |
| Purpose | Future Cloud Run entitlement API runtime service identity |
| Unique ID | Pending verification; provided evidence does not unambiguously identify the service account unique ID |
| User-managed keys | `0` |
| Service account key created | No |
| JSON key downloaded | No |

Confirmed IAM binding:

| Role | Role ID | Scope | IAM condition | Purpose |
| --- | --- | --- | --- | --- |
| Cloud Datastore User (Cloud Datastore 使用者) | `roles/datastore.user` | Project-level | None | Future Cloud Run backend read/write access to Firestore documents |

The Console evidence provided by Brian records that the Service Accounts page
shows this enabled account, correct display name/email, and `沒有任何金鑰`. The
IAM edit page shows the above account as the principal with exactly the Cloud
Datastore User role and no IAM condition or additional roles. Treat Brian's
Console screenshots as evidence; do not copy any sensitive credential visible
in a screenshot.

Explicitly not granted / not configured:

- Owner, Editor, Cloud Datastore Owner, Service Account Admin, and Service
  Account Token Creator are not granted.
- Secret Manager Secret Accessor is not granted. Grant it only later, after an
  actual secret exists, at the specific secret resource level.
- Cloud KMS CryptoKey Encrypter/Decrypter is not granted. Grant it only later,
  after an actual CryptoKey exists, at the specific CryptoKey resource level.
- Android Publisher / Google Play Console permissions were completed on
  `2026-07-10`; see Section 11.1.

Current restrictions remain:

- Do not create a service account key or download a JSON key.
- Do not create Secret Manager secrets or KMS keys.
- Do not deploy a Cloud Run service.
- Do not create Firestore collections/documents or modify Android app code.

## 9. Console Step-by-Step: Secret Manager (Completed 2026-07-10)

目的：建立 backend-only peppers。這些 secret 只給 Cloud Run runtime service
account 讀取，不給 Android，不進 repo，不進 log。

Completed pepper secrets (names only; values are not recorded)：

- `just-notes-token-hash-pepper-dev`
- `just-notes-obfuscated-account-pepper-dev`
- `just-notes-email-hash-pepper-dev`

操作步驟（已完成的 Console record）：

1. 到 Google Cloud Console -> Security -> Secret Manager。
2. 確認在 dev backend project。
3. 建立 `just-notes-token-hash-pepper-dev`。
4. 建立 `just-notes-obfuscated-account-pepper-dev`。
5. 建立 `just-notes-email-hash-pepper-dev`。
6. 每個值都應該是 high entropy random secret。
7. 不要把 secret value 貼到 repo。
8. 不要把 secret value 貼到 log。
9. 不要把 secret value 給 Android。
10. 只授予 Cloud Run service account `Secret Manager Secret Accessor`。

請記錄：

- Secret names。
- Secret versions。
- 哪個 service account 有 accessor 權限。
- Console 截圖，secret value 必須遮住。

安全要求：

- 不重用 OAuth / Drive secret 當 pepper。
- 不把 pepper 寫進 Android source。
- 不把 pepper 寫進 Gradle file。
- 不把 pepper 寫進 example config。
- 之後 backend records 要保留 `hashVersion` / `pepperVersion`。

### 9.1 Secret Manager Completion Record - 2026-07-10

Secret Manager setup is completed in the reused GCP project:

| Setting | Confirmed value |
| --- | --- |
| Project | `Gemini API` / `gen-lang-client-0599059254` / `1025325431669` |
| Project decision | Reuse existing project |
| Secret Manager API | Enabled |
| Setup completed date | `2026-07-10` |
| Replication | Automatic |
| Encryption | Google-managed |
| Expiration | Never |
| Secret values | Not recorded |

| Secret name | Version | Version status |
| --- | --- | --- |
| `just-notes-token-hash-pepper-dev` | `1` | Enabled |
| `just-notes-obfuscated-account-pepper-dev` | `1` | Enabled |
| `just-notes-email-hash-pepper-dev` | `1` | Enabled |

Each secret has its own high-entropy random value. This document records only
the safe metadata above: never read, display, copy, request, or store a secret
value in Android source, Gradle, repo, docs, memory, chat, or logs.

Confirmed resource-level IAM binding for each of the three individual Secrets:

| Principal | Role | Role ID | Scope | IAM condition |
| --- | --- | --- | --- | --- |
| `just-notes-entitlement-api-dev@gen-lang-client-0599059254.iam.gserviceaccount.com` | Secret Manager Secret Accessor | `roles/secretmanager.secretAccessor` | Resource-level, separately granted on all three Secrets | None |

Brian-provided Console evidence includes the three-secret list, the Version
pages (version number/status), and each individual Secret's IAM permissions
page. Each IAM page shows the runtime service account and the Secret Accessor
role. Do not save or process any screenshot that displays a Secret payload.

Authorization boundaries:

- The runtime service account has no project-level Secret Manager Secret
  Accessor and no Secret Manager Admin, Owner, or Editor role.
- `yeh.shibang@gmail.com` has an Owner role inherited from the `Gemini API`
  project. This is not a role of the runtime service account and must not be
  recorded as one.
- Keep the backend record design for `hashVersion` / `pepperVersion`; secret
  values themselves never enter the application repository or logs.

Current restrictions remain: do not create a service account key or download a
JSON key, deploy Cloud Run, create a KMS key, create Firestore collections or
documents, or modify Android app code.

## 10. Console Step-by-Step: KMS / Tink (Completed 2026-07-10)

目的：準備 future purchaseToken encryption。backend 之後只儲存
`tokenCiphertext`，不儲存 raw purchaseToken。

建議設定：

- Key ring: `just-notes-dev`
- Key: `just-notes-token-encryption-dev`
- Region: `asia-east1`

操作步驟：

1. 到 Google Cloud Console -> Security -> Key Management。
2. 確認 Cloud KMS API 已 enabled。
3. 建立 key ring：`just-notes-dev`。
4. Region 選 `asia-east1`。
5. 在 key ring 中建立 key：`just-notes-token-encryption-dev`。
6. Key 用途：encrypt/decrypt purchaseToken `tokenCiphertext`。
7. Cloud Run service account 只授予 CryptoKey Encrypter/Decrypter。
8. 不授予不必要的 KMS admin。
9. 不 export key material。
10. 本階段只 setup，不實際處理 real token。

請記錄：

- KMS key ring name。
- KMS key name。
- KMS region。
- KMS key resource path。
- 哪個 service account 有 encrypter/decrypter 權限。
- Console 截圖。

Future backend fields：

- `tokenCiphertext`
- `keyVersion`
- `encryptedAt`
- `encryptionAlgorithm`

### 10.1 KMS Completion Record - 2026-07-10

Cloud KMS setup is completed in the reused GCP project:

| Setting | Confirmed value |
| --- | --- |
| Project | `Gemini API` / `gen-lang-client-0599059254` / `1025325431669` |
| Project decision | Reuse existing project |
| Cloud KMS API | Enabled |
| Setup completed date | `2026-07-10` |
| Key ring | `just-notes-dev` |
| CryptoKey | `just-notes-token-encryption-dev` |
| Location | `asia-east1` |
| Status | Available |
| Protection level | Software |
| Purpose | Symmetric encrypt/decrypt |
| Algorithm | Google symmetric encryption key |
| Key material | Generated and retained by Cloud KMS; not exported |
| Rotation period | 90 days |
| Next rotation | `2026-10-08` |
| Scheduled-for-destruction duration | 30 days |

CryptoKey resource path:

`projects/gen-lang-client-0599059254/locations/asia-east1/keyRings/just-notes-dev/cryptoKeys/just-notes-token-encryption-dev`

Confirmed resource-level IAM binding on this individual CryptoKey:

| Principal | Role | Role ID | Scope | IAM condition |
| --- | --- | --- | --- | --- |
| `just-notes-entitlement-api-dev@gen-lang-client-0599059254.iam.gserviceaccount.com` | Cloud KMS CryptoKey Encrypter/Decrypter | `roles/cloudkms.cryptoKeyEncrypterDecrypter` | Resource-level, on this CryptoKey only | None |

Brian-provided Console evidence records the enabled KMS API, the key ring and
CryptoKey list, the available Software symmetric key with its 90-day rotation,
and the CryptoKey Permissions page. The Permissions page shows the runtime
service account with the Encrypter/Decrypter role and no inherited source.
`yeh.shibang@gmail.com` separately has an Owner role inherited from the
`Gemini API` project; that inherited Owner role is not a role of the runtime
service account.

Authorization and handling boundaries:

- The runtime service account has no project-level Cloud KMS
  Encrypter/Decrypter grant and no Cloud KMS Admin, Owner, or Editor role.
- No service account key or JSON credential was created or downloaded.
- No KMS key material was imported or exported.
- No real `purchaseToken` was encrypted, decrypted, stored, or otherwise
  processed during setup.
- Future backend records must store `tokenCiphertext`, `keyVersion`,
  `encryptedAt`, and `encryptionAlgorithm`; they must not store a raw
  `purchaseToken`.

Current restrictions remain: do not deploy Cloud Run, create Firestore
collections/documents, process a real purchase token, or modify Android app
code for this setup-only step.

## 11. Play Console Step-by-Step: API Access (Completed 2026-07-10)

目的：讓 future backend runtime service account 能透過 Android Publisher API
讀取 subscriptionsv2，並在後續版本支援 backend acknowledge。

操作步驟：

1. 到 Play Console。
2. 選 Just Notes app。
3. 確認 package name 是 `com.brianyeh.justnotes`。
4. 到開發人員帳戶的「使用者和權限」。
5. 確認 Android Publisher API 的 GCP consumer project；目前官方流程不再要求
   將 Play developer account 連結到 GCP project。
6. 確認 Android Publisher API 已啟用。
7. 加入 Cloud Run service account，或指定要給 API access 的 service account。
8. 對 `com.brianyeh.justnotes` 給 app-level 權限。
9. 權限目的：
   - `subscriptionsv2.get`
   - future subscriptions acknowledge
10. 不建立 service account key。
11. 不下載 service account JSON key。
12. 不授予不必要的 owner/editor。
13. 如果 Console 顯示的 API access 頁面和預期不同，記錄截圖，不要硬猜。

請記錄：

- Android Publisher API consumer GCP project ID。
- Consumer project number。
- Service account email。
- App-level permission summary。
- Android Publisher API enabled / not enabled。
- Console 截圖。

安全要求：

- Android Publisher API 權限在 Play Console「使用者和權限」管理。
- 不靠 GCP IAM owner/editor 當替代方案。
- Android app 不得包含 Play API credential。
- Repo 不得包含 service account key。

### 11.1 Play API Access Completion Record - 2026-07-10

The Play API runtime identity is active and restricted to the Just Notes app.
The final state was verified through both the Android Publisher API and Play
Console.

| Setting | Confirmed value |
| --- | --- |
| Play developer account ID | `6829900431527137752` |
| App | `Just Notes` |
| Package name | `com.brianyeh.justnotes` |
| Play Console app ID | `4973355528263748287` |
| GCP API consumer project | `Gemini API` / `gen-lang-client-0599059254` / `1025325431669` |
| Android Publisher API | Enabled on `2026-07-10` |
| Runtime service account | `just-notes-entitlement-api-dev@gen-lang-client-0599059254.iam.gserviceaccount.com` |
| Play user status | `ACCESS_GRANTED` / 有效 |
| Access expiry | Never expires |
| Permission scope | App-level only, for `com.brianyeh.justnotes` |
| Account-level permissions | None |
| Service account keys | `0`; no JSON key created or downloaded |

Confirmed app-level grant:

| Play Console permission | API enum | Purpose |
| --- | --- | --- |
| 查看應用程式資訊 (唯讀) | `CAN_VIEW_NON_FINANCIAL_DATA` | Read app information required by the backend workflow |
| 查看應用程式品質資訊 (唯讀) | `CAN_VIEW_APP_QUALITY` | Read-only app quality access automatically paired by Play Console |
| 查看財務資料 | `CAN_VIEW_FINANCIAL_DATA` | Use the Purchases API, including future `subscriptionsv2.get` verification |
| 管理訂單和訂閱項目 | `CAN_MANAGE_ORDERS` | Support the future backend subscription acknowledgement workflow |

Current Google documentation states that linking the Play developer account to
a GCP project is no longer required. The project recorded above is the API
consumer project where `androidpublisher.googleapis.com` is enabled; see the
[Google Play Developer API getting-started guide](https://developers.google.com/android-publisher/getting_started?hl=en).

The Play Console invitation form kept its final action disabled, so the same
official Android Publisher `users` and `grants` resources were used to create
the service-account Play user and its app grant. Because `User.grants` is an
output-only field, user creation temporarily required
`CAN_VIEW_NON_FINANCIAL_DATA_GLOBAL`; the Just Notes app grant was then created
and that temporary developer-account permission was immediately removed.
Final API verification returned `GLOBAL=[]`, one grant for
`com.brianyeh.justnotes`, and exactly the four permissions listed above.

Security and cleanup verification:

- No account-level, admin, owner, editor, release, store-listing, review,
  policy, or deep-link permission remains on the service account.
- No service account key or JSON credential was created, downloaded, or stored
  in the Android app or repository.
- Temporary OAuth Application Default Credentials were revoked, their local
  credential file was removed, and temporary API response/request files were
  deleted after verification.
- No real purchase, purchase token, subscription acknowledgement, refund, or
  cancellation was performed.

Console evidence:

- [Service-account app assignment summary](evidence/v1.0.6-C-play-user-access-2026-07-10.png)
- [App access and read-only permissions](evidence/v1.0.6-C-play-api-access-2026-07-10.png)
- [Financial and order/subscription permissions](evidence/v1.0.6-C-play-api-access-financial-2026-07-10.png)

## 12. Play Console Step-by-Step: Testers (Completed 2026-07-10)

目的：確認未來 v1.0.7 purchase validation 會用到的 tester setup，但本階段不做
purchase test。

確認項目：

- Internal testers 是否包含 Brian 要用的測試帳號。
- License testers 是否包含 Brian 要用的測試帳號。
- 測試帳號是否能從 Play Store 安裝測試版本。
- Play Store 安裝版本是否對應未來要測的 build。

本階段限制：

- 不做 license tester purchase。
- 不做真實 purchase test。
- 不打開 purchase button。
- 不讓 `launchBillingFlow` reachable。
- License tester purchase 留到 v1.0.7。

請記錄：

- Internal testers 狀態。
- License testers 狀態。
- Play Store 安裝版本狀態。
- Console 截圖。

### 12.1 Tester Setup Completion Record - 2026-07-10

Internal testing, license testing, and Play Store tester access are configured
and verified for the existing Brian tester list. The personal tester email is
not repeated in this document.

| Setting | Confirmed value |
| --- | --- |
| App | `Just Notes` / `com.brianyeh.justnotes` |
| Internal testing track | Active |
| Internal track ID | `4701127794548238354` |
| Selected internal tester list | `Brian` |
| Internal tester count | `1` |
| Internal release | Version code `4`, version name `1.0.3` |
| Internal release status | Available to internal testers; not yet reviewed |
| Internal tester opt-in | Confirmed by Google Play: `You're a tester` |
| Opt-in URL | `https://play.google.com/apps/internaltest/4701127794548238354` |
| Play Store test listing | Available; `Download test app` and `Install on more devices` are shown |
| Play Store delivered version | `1.0.3` |
| Device compatibility | Version `1.0.3` works on multiple active Android devices; requires Android 8.0 or later |
| Selected license tester list | `Brian` |
| License tester count | `1` |
| License response | `RESPOND_NORMALLY` |
| License setting save result | Saved successfully |

The currently available Play Store test build matches the existing internal
release (`1.0.3`, version code `4`). It is the setup-validation baseline, not
the future v1.0.7 purchase-validation build. Before v1.0.7 purchase testing, a
new v1.0.7 test build must be uploaded to this internal track and its delivered
version must be checked again.

Verification and safety boundaries:

- The Brian list was already selected for internal testing; no duplicate list
  or tester was created.
- The Brian list was newly selected and saved for license testing.
- The signed-in test account is already enrolled in the internal test and can
  reach the Play Store test listing.
- No remote device installation or APK installation was started.
- No purchase button, `launchBillingFlow`, license-tester purchase, real
  purchase, refund, cancellation, restore, or acknowledgement was performed.
- No version code/name or internal release artifact was changed in this step.

Console and Play Store evidence:

- [Internal testing release status](evidence/v1.0.6-C-internal-test-version-2026-07-10.png)
- [Selected internal tester list](evidence/v1.0.6-C-internal-testers-2026-07-10.png)
- [Selected license tester list and normal response](evidence/v1.0.6-C-license-testers-2026-07-10.png)
- [Google Play internal-test enrollment](evidence/v1.0.6-C-internal-test-opt-in-2026-07-10.png)
- [Play Store delivered version and device compatibility](evidence/v1.0.6-C-play-store-test-build-2026-07-10.png)

## 13. Required Values To Capture (Completed 2026-07-10)

下面是已完成並驗證的 non-secret values。不要加入 secret value、client secret、
JSON key、raw purchaseToken 或 raw Google ID token。

| Value | Confirmed value | Verified source |
| --- | --- | --- |
| GCP project ID | `gen-lang-client-0599059254` | Cloud Console project selector |
| GCP project number | `1025325431669` | Project settings |
| OAuth Web client ID | `1025325431669-l4rkq07l3f1rq3r7u0hbjd238a0te79p.apps.googleusercontent.com` | APIs & Services -> Credentials |
| Cloud Run service account email | `just-notes-entitlement-api-dev@gen-lang-client-0599059254.iam.gserviceaccount.com` | IAM / Service Accounts |
| Firestore region | `asia-east1` | Firestore |
| Secret names created | `just-notes-token-hash-pepper-dev`<br>`just-notes-obfuscated-account-pepper-dev`<br>`just-notes-email-hash-pepper-dev` | Secret Manager |
| KMS key resource | `projects/gen-lang-client-0599059254/locations/asia-east1/keyRings/just-notes-dev/cryptoKeys/just-notes-token-encryption-dev` | KMS |
| Play API access status | `ACCESS_GRANTED`; 4 Just Notes app permissions; no account-level permissions | Play Console -> 使用者和權限 |
| Tester setup status | Internal and license lists active; Play Store test version `1.0.3` available | Play Console testing / license testing / Play Store |
| Play App Signing SHA-1/SHA-256 | SHA-1: `A9:66:E3:16:A5:8F:F0:5C:D3:9F:3B:78:98:CB:D2:20:70:BA:74:D9`<br>SHA-256: `B4:17:55:03:7F:E3:5B:A2:42:C3:DA:82:FD:07:E9:14:D4:A8:75:50:18:A4:C8:60:C1:20:90:E2:75:1B:EF:09` | Play Console -> 由 Google Play 保護 -> 應用程式簽署 |

已安全記錄的 non-secret evidence：

- Project ID / project number。
- Web client ID。
- Service account email。
- Firestore region。
- Secret names。
- KMS key resource path。
- Play API access linked / permission status。
- Tester setup status and non-secret test version metadata。
- SHA-1 / SHA-256 fingerprint values。

不要貼回來：

- Service account JSON key。
- OAuth client secret。
- Secret Manager value。
- Raw purchaseToken。
- Raw Google ID token。
- Raw googleSub。
- Raw email。

### 13.1 Required Values Completion Record - 2026-07-10

All values required by this section have been captured. Project, Firestore,
Secret Manager, KMS, Play API access, and tester values come from the
completion records in Sections 6-12 and were cross-checked against the
corresponding Console state. The remaining live checks completed here were the
full OAuth Web client ID and the Play App Signing public certificate
fingerprints.

Security handling:

- The OAuth Web client ID is a non-secret identifier. The OAuth client secret
  was not opened, read, copied, recorded, or included in a screenshot.
- The recorded SHA-1 and SHA-256 values belong to the public Play App Signing
  certificate, not to private signing-key material.
- No certificate, signing key, service-account key, JSON credential, secret
  payload, raw purchase token, Google ID token, or personal tester email was
  downloaded or added to the repository.
- The upload-key certificate is separate from the Play App Signing
  certificate; only the Play App Signing fingerprints required by this table
  are recorded above.

Console evidence:

- [Play App Signing public certificate fingerprints](evidence/v1.0.6-C-play-app-signing-fingerprints-2026-07-10.png)
- [Play App Signing SHA-256 fingerprint continuation](evidence/v1.0.6-C-play-app-signing-sha256-end-2026-07-10.png)

## 14. What Must Not Be Done Yet

本階段不要做：

- 不開 purchase button。
- 不開 `launchBillingFlow`。
- 不做 license tester purchase。
- 不做 RTDN。
- 不做 restore。
- 不做 real acknowledge lifecycle。
- 不部署 production。
- 不部署 Cloud Run。
- 不把 key / secret 放 repo。
- 不下載 service account JSON key。
- 不把 service account key 放 Android app。
- 不讓 `POST /v1/billing/verify` grant Premium。
- 不讓 `POST /v1/billing/verify` 回 `hasPremium=true`。
- 不建立 Android runtime `POST /v1/billing/verify` call path。
- 不改 versionCode / versionName。
- 不碰 emulator。
- 不安裝 APK。
- 不跑 `connectedDebugAndroidTest`。
- 不碰既有 untracked `Doc/billing/`。

## 15. Next Step After Brian Completes Console Setup (Config Wiring Completed 2026-07-10)

Brian 完成 Console setup / verification 後，下一步順序：

1. Required Values table 已完成；不需要再貼回已記錄的 non-secret values。
2. Limited-scope read-only assessment completed；沒有 config wiring blocker。
3. v1.0.6-C config wiring plan 已完成並實作。
4. Config wiring implementation 維持 fail-closed 並通過 local unit tests。
5. Purchase flow 繼續留到 v1.0.7。
6. `POST /v1/billing/verify` 是否改為 granting 必須等後續明確批准；目前仍
   non-granting。

v1.0.6-C 後續任何 implementation pass 都必須維持：

- Missing config fail closed。
- Unknown fail closed。
- `VerificationPending` no grant。
- Ownership mismatch no grant。
- Unacknowledged no grant。
- Expired no grant。
- Android 不知道 backend pepper。
- Android 不持有 service account credential。

### 15.1 Config Wiring Completion Record - 2026-07-10

The limited config-wiring phase is implemented. Detailed decisions and the
deployment boundary are recorded in
[`v1.0.6-C-config-wiring-plan.md`](v1.0.6-C-config-wiring-plan.md).

| Item | Completed state |
| --- | --- |
| Android Google Web client ID | Wired from the checked-in non-secret `justNotes.googleWebClientId` Gradle property |
| Android backend URL | Explicitly blank; backend entitlement HTTP client remains disabled |
| Android build-time validation | Rejects malformed Web client IDs and non-HTTPS/path-bearing/credential-bearing/query-bearing/fragment-bearing backend origins |
| Android runtime validation | Rejects invalid non-empty config before opening a connection or sending an ID token |
| Backend dev runtime reference | `backend/config/dev.env.example` contains only non-secret consumed environment variables |
| Backend ID-token config | Missing or malformed Google Web client ID fails closed |
| Backend verify route | Still returns `hasPremium=false`, `VerificationPending`, and `POST_VERIFY_DISABLED` |
| Play verifier | Still `NoopPlaySubscriptionVerifier` |
| Entitlement repository | Still non-production in-memory implementation |
| Cloud deployment | Not performed |

Local verification completed with:

```powershell
./gradlew.bat :backend:test :app:testDebugUnitTest --no-daemon
```

Result: `BUILD SUCCESSFUL`. The added coverage verifies that a blank endpoint
stays disabled with the Web client ID present, and that missing or malformed
identity configuration fails before any network connection opens.

## 16. Config Wiring Delivery Checklist

完成 config wiring 後應驗證：

- `git diff --check`
- 確認只提交本階段的 config、tests、docs 與 non-secret Console evidence。
- 確認沒有碰 `Doc/billing/`。
- 確認沒有部署 Cloud Run、沒有 Firestore write、沒有 secret/KMS payload
  access、沒有 purchase flow、沒有 granting。
- 執行 backend 與 Android unit tests。
- 執行 `codex xhigh/review`，修正任何 blocking finding。
- Build 最新 debug APK 並複製到既定 Google Drive 測試路徑。
- Review staged diff 後才 commit / push，且不得包含既有 unrelated changes。
