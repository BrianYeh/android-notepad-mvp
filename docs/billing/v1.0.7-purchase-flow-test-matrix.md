# Just Notes v1.0.7 Purchase Flow Test Matrix

Status: implementation verification in progress

Branch: `codex/v1.0.7-purchase-orchestration`
Last updated: 2026-07-11

## Security and grant invariants

- Android never grants Premium from `PendingPurchase` or `ClientObserved` state.
- Only a backend-verified, unexpired, grantable, acknowledged entitlement grants Premium.
- The raw purchase token is transient: it is sent to the authenticated backend, never persisted by Android, never logged, and never stored raw in Firestore.
- The backend checks Google identity, Play truth, catalogue, backend-issued obfuscated account ownership, and unique token ownership before acknowledgement or entitlement grant.
- A retry delay retains only a timer; the next Play query reacquires the token.
- RTDN remains disabled (`501`) in v1.0.7.

## Automated backend cases

| Case | Expected HTTP | Stored subscription | Acknowledgement | Premium access | Evidence |
|---|---:|---|---|---|---|
| Valid active monthly `trial10d` | 200 | Ciphertext + key metadata; no raw token | Acknowledged | Yes, after acknowledgement | `PurchaseFlowAcceptanceTest.successPersistsEncryptedTokenAcknowledgesAndGrantsOnlyAtTheEnd` |
| Play Pending | 202 | Non-granting encrypted record | Not attempted | No | `pendingNeverAcknowledgesOrGrantsPremium` |
| Same-owner repeat | 200 | Existing ciphertext reused | Idempotent | Yes | `sameOwnerRepeatIsIdempotentAndOtherOwnerConflictHasNoSensitiveWrites` |
| Token already bound to another owner | 409 | No attacker-owned write | Not attempted | No | same test; client response contains neither owner nor token |
| Retryable acknowledgement failure | 202 | Pending + attempt/backoff metadata | Retry scheduled | No | `retryableAcknowledgementFailureRetriesLaterAndOnlyThenGrants` |
| Later acknowledgement success | 200 | Same encrypted record updated | Acknowledged | Yes | same test |
| Expired | 200 | Expired lifecycle record | Not required | No | `expiredRevokedAndMalformedRequestsRemainNonPremium` |
| Revoked | 200 | Revoked lifecycle record | Not required | No | same test |
| Malformed verify body | 400 | None | Not attempted | No | same test |
| Ownership mismatch / missing ID | 422 | No sensitive write | Not attempted | No | `EntitlementRoutesTest`, `BillingVerificationOrchestratorTest` |
| Catalogue/package mismatch | 422 | No grant | Not attempted | No | `EntitlementRoutesTest`, `BillingVerificationOrchestratorTest` |
| Play dependency unavailable | 503 | No grant | Not attempted | No | `EntitlementRoutesTest` |
| Canceled pending replacement linked to an active plan | 200 | Canceled candidate remains non-granting | Not attempted | Existing active plan is preserved | `EntitlementRepositoryTest.canceledReplacementPreservesTheLinkedActiveEntitlement` |
| Newer successor revoked while an older-token verification finishes late | 200 | Successor relationship remains on entitlement | Not attempted | No; stale result cannot restore Premium | `EntitlementRepositoryTest.staleOldTokenCannotRestorePremiumAfterItsNewerSuccessorWasRevoked` |
| Legacy entitlement and candidate both have null linkage | 200 | No false token relationship | Not attempted | Freshness/grant rules apply normally | `EntitlementRepositoryTest.nullLinkedHashDoesNotMatchAnExistingLegacyEntitlementWithoutATokenHash` |

## Automated Android cases

| Case | Expected result | Evidence |
|---|---|---|
| Default build flag off | Purchase launch disabled | `PremiumPurchaseGateTest.normalDebugBuildKeepsBackendPurchaseFlowDisabled` |
| Feature enabled, backend/account not ready | Disabled | `enabledFlowCanLaunchOnlyAfterAccountBackendAndBillingAreReady` |
| Context or verify in flight | Disabled; re-enabled after safe failure | `PremiumBillingStateTest`, `NoteUiPureFunctionTest` |
| Monthly exactly one `trial10d` | Trial offer selected and trial copy allowed | `PremiumCatalogTest` |
| Monthly no trial | Exactly one no-offer base plan selected | `PremiumCatalogTest` |
| Annual | Exactly one no-offer annual plan; no trial promise | `PremiumCatalogTest`, `UiTextTest` |
| Duplicate or wrong catalogue offers | Fail closed | `PremiumCatalogTest` |
| Purchased callback | Emits transient candidate with launch hints | `PremiumBillingStateTest` |
| Restore callback | Emits every Purchased candidate with null hints | `PremiumBillingStateTest` |
| Duplicate token in flight | One POST; later retry allowed after completion | `BackendPurchaseRepositoryTest` |
| Account switch or stale response | Response discarded | `BackendEntitlementRepositoryTest` |
| Pending / ClientObserved Purchased | No Premium-only access | `PremiumPurchaseGateTest.pendingAndClientObservedPurchasedNeverUnlockPremium` |

## Verification commands and results

| Command | Result |
|---|---|
| `:backend:test --tests "*PurchaseFlowAcceptanceTest"` | PASS (5 tests, 2026-07-11) |
| `:app:testDebugUnitTest` | PASS (Task 8, 2026-07-11) |
| `:backend:test :app:testDebugUnitTest :app:lintDebug :app:assembleDebug` | PASS (2026-07-11; lint blocker corrected by marking the brand name non-translatable) |
| `PremiumPurchaseGateTest` on `LocalNotepad_API35` | PASS (3 tests, Android 15, 2026-07-11) |
| Repository secret/log scan | PASS; only explicit `[REDACTED]` purchase-token `toString` output matched |
| Debug APK Drive handoff | PASS; source and Drive copy SHA-256 `83A321B79225778FC459674179C7651F005CE062C854A7AA87B4A14F4CC0C739` |
| `docker build -t just-notes-entitlement-backend:v1.0.7 backend` | PASS in WSL Docker after final review fixes; manifest-list SHA-256 `67b11c7143f6a52ac98b25beaf3480de5c9472a4b2d825df57b712b38dc9986f` |
| First `codex xhigh/review` | Found two P1 reconciliation defects: unrelated non-granting tokens could replace Premium and canceled Pending was not terminal |
| Post-fix `codex xhigh/review --uncommitted` | Found two P1 and one P2 follow-up defects: canceled replacement could remove the linked active plan, stale old-token results could restore a revoked successor, and null hashes could compare equal |
| Second post-fix `codex xhigh/review --uncommitted` | Found three P1 ordering/horizon defects: canceled successor first-write ordering, stale Premium beyond `maxStale`, and older Premium beating newer terminal denial |
| Third post-fix `codex xhigh/review --uncommitted` | Found two P1 tie/transition defects: linked Pending replacement could suppress its active predecessor and equal-time terminal denial was not symmetric |
| Fourth post-fix `codex xhigh/review --uncommitted` | Found one P1 completion-order defect for unrelated transient tokens and one P2 canceled-Pending mapper defect with pending acknowledgement |
| Fifth post-fix `codex xhigh/review --uncommitted` | Found one P1 three-step ordering defect where transient state could erase terminal-denial history before an older Premium result arrived |
| Sixth post-fix `codex xhigh/review --uncommitted` | Found the reverse-order P1 variant where an older terminal observation arrived after a newer transient record |
| Final `codex xhigh/review --uncommitted` | CLEAN; no actionable regressions. Reviewer independently ran 64 focused tests with 0 failures |
| Focused reconciliation and concurrency tests after follow-up fixes | PASS (2026-07-11) |
| Full `:backend:test` after latest follow-up fixes | PASS (148 tests, 0 failures, 2026-07-11) |
| Post-review `:backend:test :app:testDebugUnitTest :app:lintDebug :app:assembleDebug` | PASS (2026-07-11) |

## Manual Cloud Run canary gates

- Capture the current serving revision before deployment.
- Deploy a tagged `v1-0-7` revision with `--no-traffic`.
- Tagged `/v1/health` returns 200.
- Tagged context and verify without authorization return 401.
- Tagged RTDN returns 501.
- Authenticated context returns a 43-character Base64URL account ID.
- Logs contain request IDs and stable error codes, but no bearer token, raw purchase token, email, Google subject, ciphertext, or upstream exception body.
- Shift traffic only after every smoke check passes.

## Manual Play internal-test cases

| Case | Expected result | Status |
|---|---|---|
| Play-delivered monthly purchase | Pending until backend success; then acknowledged Premium | Pending signed AAB/upload |
| Eligible `trial10d` | Trial appears only if Play marks tester eligible | Pending |
| Ineligible monthly fallback | No-offer monthly base plan launches | Pending |
| User cancellation | No candidate grant and no Premium | Pending |
| Play Pending simulator | Non-Premium Pending state | Pending |
| Retryable Play/backend failure | Safe non-Premium error; later restore retries | Pending |
| Relaunch restore | Same-owner idempotent verify restores Premium | Pending |
| Clear app data + sign in + restore | Play reacquires token; no local token dependency | Pending |
| Cancel before expiry | `CanceledActiveUntilExpiry`, access until expiry | Pending |
| Accelerated tester expiry | `Expired`, Premium removed after requery | Pending |
| Annual no-offer | No trial wording/token | Pending |

## Rollback conditions

Immediately stop testing, disable the internal release/backend-flow flag, and route Cloud Run back to the captured prior revision if any of these occurs:

- ownership mismatch or first-binder attempt grants;
- acknowledgement failure grants;
- Pending, expired, revoked, or ClientObserved state grants;
- raw purchase token, bearer token, Google subject/email, or ciphertext appears in logs or screenshots;
- Firestore stores a raw token or lacks encryption/key metadata;
- authenticated context, health, or fail-closed auth smoke checks fail;
- the Play-delivered build differs from version code 5 / version name 1.0.7 or enables client-only entitlement.

Rollback command template:

```powershell
gcloud run services update-traffic just-notes-entitlement-api-dev `
  --project gen-lang-client-0599059254 `
  --region asia-east1 `
  --to-revisions "$previousRevision=100"
```

## RTDN limitation

RTDN is intentionally not implemented in this branch and `/v1/play/rtdn` must remain `501`. Until a separate reviewed RTDN plan is implemented, off-device renewal, refund, revocation, and cancellation are learned only through authenticated app-driven Play requery/restore. v1.0.7 cannot be marked complete without making this limitation visible; it must not be hidden by trusting notification payloads or client state.
